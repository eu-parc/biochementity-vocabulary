{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://w3id.org/peh/biochementity-vocabulary/schema/dropbox-biochementity.schema.json",
  "title": "Biochementity dropbox file",
  "description": "Contract for a dropbox/*.yaml file proposing one or more biochementity subclasses. Each entry maps to a LinkML BioChemEntitySubClass (defined upstream in eu-parc/parco-hbm, schema/peh.yaml).",
  "type": "object",
  "required": ["biochementity_subclasses"],
  "additionalProperties": false,
  "properties": {
    "suggester": {
      "$ref": "#/$defs/orcid",
      "description": "File-level default suggester (ORCID). Applied to every entry that does not set its own suggester."
    },
    "biochementity_subclasses": {
      "type": "array",
      "description": "The list of proposed biochementity subclasses (the TARGET_CLASS).",
      "minItems": 1,
      "items": { "$ref": "#/$defs/biochementitySubClass" }
    }
  },
  "$defs": {
    "orcid": {
      "type": "string",
      "description": "Canonical ORCID URL, e.g. https://orcid.org/0000-0002-1825-0097.",
      "pattern": "^https://orcid\\.org/[0-9]{4}-[0-9]{4}-[0-9]{4}-[0-9]{3}[0-9X]$"
    },
    "contextAlias": {
      "type": "object",
      "additionalProperties": false,
      "required": ["property_name", "alias"],
      "properties": {
        "property_name": { "type": "string", "description": "The entity property the alias applies to." },
        "context": { "type": "string", "description": "The scope/context (community, project, study) in which the alias is used." },
        "alias": { "type": "string", "description": "The alternative term used in that context." }
      }
    },
    "translation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["property_name", "language", "translated_value"],
      "properties": {
        "property_name": { "type": "string", "description": "The entity property being translated." },
        "language": { "type": "string", "description": "Language tag of the translation, e.g. nl, de, fr." },
        "translated_value": { "type": "string", "description": "The translated term." }
      }
    },
    "biochementitySubClass": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Subject IRI or CURIE (schema:identifier). Optional; minted on the fly if absent. An existing valid URI is kept."
        },
        "name": {
          "type": "string",
          "description": "Preferred label (rdfs:label). Effectively required."
        },
        "short_name": {
          "type": "string",
          "description": "Shortened name or code (schema:alternateName)."
        },
        "ui_label": {
          "type": "string",
          "description": "Human-readable label for UI forms/documents (pehterms:hasUiLabel)."
        },
        "description": {
          "type": "string",
          "description": "Free-text description (schema:description)."
        },
        "remark": {
          "type": "string",
          "description": "Additional comment/note/remark (schema:comment)."
        },
        "exact_matches": {
          "type": "array",
          "description": "External identifiers this term is an exact match of (skos:exactMatch), e.g. CHEBI/CAS/pubchem/inchikey.",
          "items": { "type": "string" }
        },
        "aliases": {
          "type": "array",
          "description": "Alternative names (schema:alternateName).",
          "items": { "type": "string" }
        },
        "context_aliases": {
          "type": "array",
          "description": "Context-specific aliases (pehterms:hasContextAlias).",
          "items": { "$ref": "#/$defs/contextAlias" }
        },
        "translations": {
          "type": "array",
          "description": "Translations of entity properties (pehterms:hasTranslation).",
          "items": { "$ref": "#/$defs/translation" }
        },
        "grouping_id_list": {
          "type": "array",
          "description": "Groupings this term is broader-related to (skos:broader).",
          "items": { "type": "string" }
        },
        "group_labels": {
          "type": "array",
          "description": "Group labels (pehterms:hasGroupLabel).",
          "items": { "type": "string" }
        },
        "molweight_grampermol": {
          "type": "number",
          "description": "Molecular weight in g/mol (pehterms:hasMolecularWeight)."
        },
        "has_role": {
          "type": "array",
          "description": "External CHEBI roles (RO_0000087).",
          "items": { "type": "string" }
        },
        "is_metabolite_of": {
          "type": "array",
          "description": "Internal link: this entity is a metabolite of another biochementity (pehterms:isMetaboliteOf).",
          "items": { "type": "string" }
        },
        "is_isomer_of": {
          "type": "array",
          "description": "Internal link: this entity is an isomer of another biochementity (pehterms:isIsomerOf, symmetric).",
          "items": { "type": "string" }
        },
        "parent_biochementities": {
          "type": "array",
          "description": "Superclasses (rdfs:subClassOf); usually https://w3id.org/peh/terms/BioChemEntity, may be another biochementity. Multiple allowed.",
          "items": { "type": "string" }
        },
        "suggester": {
          "$ref": "#/$defs/orcid",
          "description": "Per-entry suggester (ORCID, prov:wasAttributedTo). Overrides any file-level default."
        }
      }
    }
  }
}
