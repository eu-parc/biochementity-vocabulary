# nanopub-preview — TEMPORARY review artifact

**This folder is not meant to be merged.** It is committed only so the migrated
nanopublications can be double-checked in-place, and will be **removed before the
PR is merged** (so the squash-merge nets it out of the upstream history).

## Files

- **`introduction.trig`** — a **throw-away** bot introduction (key declaration).
  Declares the publishing agent `…/biochementity-bot`, owned by
  `orcid:0000-0001-8327-0142` (Gertjan Bisschop), self-signed with its key. The
  real bot identity will be generated separately; this one only exists to preview
  the structure and to sign the nanopubs below.
- **`nanopubs.trig`** — all 900 migrated nanopublications (879 defining + 21
  superseding) concatenated into one file for easy reading. Generated by
  `pubmate-migrate` over [`../unpublished/`](../unpublished).

## What to check

- **Suggester** (in each `provenance` graph): `prov:wasAttributedTo
  orcid:0000-0001-8327-0142` — Gertjan Bisschop, contributor of all existing data.
- **Signer / publisher** (in each `pubinfo` graph): `npx:signedBy` and
  `prov:wasAttributedTo` point at the throw-away bot agent URI (the same one
  `introduction.trig` declares), so the bot-identity wiring can be checked too.
- Inter-term links resolved to the new trusty thing URIs; cyclic `isIsomerOf`
  links added by the 21 superseding nanopubs.
- Short blank-node labels (`sub:_b1`) and readable prefixes.

## Important caveats

- **Throw-away keys / not final codes.** Signed with a throw-away bot key, so the
  trusty artifact codes are **not final** — the real bot key will change every
  code. Review the *content and structure*, not the specific codes. Nothing here
  is published to any network.
- **7 dangling references were dropped** (broken `isMetaboliteOf` links to 3
  missing terms; see eu-parc/biochementity-vocabulary#45). Affected terms still
  mint, minus the broken link.
